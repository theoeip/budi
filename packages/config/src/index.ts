// @budi/config — Shared configuration
// Central export point for all configuration constants.

export { ROLES, ROLE_HIERARCHY, type RoleConfig } from './roles';
export { MODULES, FEATURE_FLAGS, type ModuleConfig } from './modules';
export { NAV_ITEMS, type NavItem } from './navigation';

